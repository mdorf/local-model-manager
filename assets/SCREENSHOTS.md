# README screenshots

Capture these during a real session and save them here with the exact filenames
below — the README embeds them by these paths.

| File | What it should show |
|---|---|
| `ui-model-list.png` | The web UI at `:8770` — sidebar listing discovered models, one selected. |
| `ui-detail.png` | The detail pane for a model: recommended config, context dropdown, editable launch flags, metadata, and the RAM fit-check badge. |
| `ui-connect.png` | The **Connect an agent** modal (the Hermes one-click + the OpenAI-compatible base URL / model id). |
| `ui-logs.png` | A running model with the live log drawer open. |

Tips: a window ~1200px wide renders cleanly; trim browser chrome.
